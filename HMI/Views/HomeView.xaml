<UserControl x:Class="HMI.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:HMI.Converters"
             xmlns:viewmodels="clr-namespace:HMI.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:HomeViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel Margin="20" HorizontalAlignment="Center">

        <TextBlock Text="Fläktkontroll" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Status -->
        <TextBlock Text="{Binding ButtonText}" 
                   FontSize="16" Margin="0,0,0,10"/>

        <!-- Start/Stop knapp -->
        <Button Content="{Binding ButtonText}"
                Command="{Binding ToggleFanCommand}"
                Width="100" Height="30" Margin="0,0,0,10"/>

        <!-- Uppdatera status -->
        <Button Content="Uppdatera status" Command="{Binding RefreshStatusCommand}" Width="150"/>

    </StackPanel>
</UserControl>